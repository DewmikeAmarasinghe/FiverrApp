import type { Config } from "@react-router/dev/config";

export default {
  // Config options...
  // For Netlify static hosting, disable SSR to output a client build
  ssr: false,
} satisfies Config;
