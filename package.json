{
  "name": "fiverr-monorepo",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "postinstall": "npm --prefix Backend ci || npm --prefix Backend install",
    "start": "npm --prefix Backend start"
  }
}


